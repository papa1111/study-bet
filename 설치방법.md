# 공부 내기판 알림 설정 (Cloudflare Worker)

사이트: https://papa1111.github.io/study-bet/
Firebase 프로젝트: study-bet-dd58b

알림 빼고는 이미 다 작동해. 아래는 **폰 알림**을 켜기 위한 설정이야. 15분 정도 걸려.

## 1. Firebase 서비스 계정 키 받기 (비밀 키, 절대 공개 금지)
1. https://console.firebase.google.com/project/study-bet-dd58b/settings/serviceaccounts/adminsdk
2. **새 비공개 키 생성** → 확인 → JSON 파일이 내려받아짐
3. 이 파일은 GitHub에 올리거나 채팅에 붙여넣지 마. 아래 3-4단계에서만 씀

## 2. Cloudflare 가입
1. https://dash.cloudflare.com/sign-up 에서 가입 (무료, 카드 필요 없음)

## 3. Worker 만들기
1. 대시보드 왼쪽 **Compute (Workers)** → **Workers & Pages** → **Create** → **Start with Hello World** (Create Worker)
2. 이름: `study-bet-push` → **Deploy**
3. **Edit code** → 기존 코드 전부 지우고 `worker/worker.js` 내용 전체 붙여넣기 → **Deploy**
4. 주소가 생김: `https://study-bet-push.<네아이디>.workers.dev`
   브라우저로 열어서 `작동 중` 문구가 나오면 성공

## 4. 설정값 등록
Worker 화면 → **Settings** → **Variables and Secrets** → **Add**

| 이름 | 종류 | 값 |
|---|---|---|
| `SERVICE_ACCOUNT` | **Secret** | 1단계 JSON 파일을 메모장으로 열어서 내용 전체 |
| `ALLOWED_ORIGIN` | Text | `https://papa1111.github.io` |
| `SITE_URL` | Text | `https://papa1111.github.io/study-bet/` |

저장(Deploy) 누르기.

## 5. Worker 주소 알려주기
3-4에서 받은 Worker 주소를 Claude한테 알려주면 `firebase-config.js`의 `workerUrl`에 넣고 GitHub에 올려줌.
(직접 하려면 `firebase-config.js`의 `workerUrl: ""` 따옴표 안에 주소 넣고 GitHub에서 파일 수정)

## 6. 폰에서 알림 켜기
- **안드로이드**: Chrome으로 사이트 열기 → 설정 탭 → **알림 켜기** → 허용
- **아이폰**: Safari로 사이트 열기 → 공유 버튼 → **홈 화면에 추가** → 홈 화면 아이콘으로 열기 → 설정 탭 → **알림 켜기** → 허용

## 문제 생기면
- 알림 켜기 버튼이 회색: `workerUrl` 이 아직 비어 있음 (5단계)
- 알림이 안 옴: Worker 화면 → **Logs**(Observability)에서 에러 확인, 에러 문구를 Claude한테 보내기
- `permission-denied`: Firestore 규칙이 최신이 아님

## 규칙 요약
- 매일 자정: 꼴찌 +500원, 최소 기준 미달 +500원 (둘 다 해당돼도 500원)
- 매주 월요일: 지난주 합계 꼴찌 +1000원. 그룹 주간 목표 달성 시 그 주 벌금 전부 면제
- 휴식일: 정산 없음 / 면제권: 1인 월 2장, 그날 일일 정산 제외
- 타이머는 본인이 끌 때까지 계속, 18시간 넘으면 자동 종료
- 구라 신고: 3명 이상, 본인 빼고 과반 동의 시 삭제, 1인 하루 3회
- 기록 수정 요청: 본인 빼고 과반 동의 시 반영
- 방장: 규칙 변경, 강퇴, 방장 넘기기, 백업
